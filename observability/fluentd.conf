<source>
  @type forward
  port 24224
  bind 0.0.0.0
</source>

<source>
  @type syslog
  port 5140
  bind 0.0.0.0
  tag system
</source>

<filter **>
  @type record_transformer
  <record>
    hostname "#{Socket.gethostname}"
    service_name "agent-framework"
  </record>
</filter>

<match **>
  @type stdout
</match>